# customfan dashboard

React + Vite live dashboard. Built out in **Stage 4** — connects to the
daemon's WebSocket (`ws://localhost:4310/ws`) for live temps, fan speed,
leak suspects, and the watchdog action log.

Placeholder until then so the repo shape is final from Stage 1.
